{% extends "AppBundle:ResponsabileProduzione:BaseOperatori.html.twig" %}

{% block title %}Riepilogo - Nuovo Operatore{% endblock %}

{% block Ocorpo %}
    <section class="t1" >
        <table class="table table-hover">
            <caption> <h4>Inserimento - <span>Operatore</span></h4></caption>
        {%for item in riepilogo%}
            <tr><th class="span5">{{item.tipo}}</th><td >{{item.contenuto}}</td></tr>
        {%endfor%}
        </table>
            <a href={{path('SPoperatori')}} id="modal_addclient" class="btn-flat primary" role="button" style="border-radius:2px;">Annulla</a>
            <a href={{ path('SPconferma')}} id="modal_addclient" class="btn-flat primary" role="button" style="border-radius:2px;">Conferma</a>
    </section>
{% endblock %}